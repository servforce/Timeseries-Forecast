# .dockerignore (放在项目根目录)

# 忽略 Python 缓存
**/__pycache__/
**/*.py[cod]
**/*.so
**/*.log

# 忽略虚拟环境
server/venv/
server/.venv/
server/env/

# ⚠️ 关键：忽略大模型文件 (加上 server/ 前缀)
server/app/models/chronos_models/
*.safetensors
*.pt
*.pth
*.ckpt

# 忽略 Git 和 Docker 配置
.git/
.gitignore
docker-compose.yaml
server/Dockerfile

# 忽略 IDE 配置
.vscode/
.idea/
.DS_Store